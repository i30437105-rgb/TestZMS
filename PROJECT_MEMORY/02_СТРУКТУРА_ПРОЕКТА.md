# 🏗️ СТРУКТУРА ПРОЕКТА

> Полная карта файлов и папок

**Источник:** Интегрировано из PROJECT_BIBLE.md + актуализировано

---

## 📁 СТРУКТУРА ПАПОК

```
TestZMS/
├── PROJECT_MEMORY/          # Память проекта (ты здесь!)
│   ├── 00_README_КРАТКО.md
│   ├── 01_ЦЕЛИ_И_ГРАНИЦЫ.md
│   ├── 02_СТРУКТУРА_ПРОЕКТА.md
│   ├── 03_ТЕКУЩИЕ_РЕШЕНИЯ.md
│   ├── 04_ЗАДАЧИ_И_ОЧЕРЕДЬ.md
│   ├── 05_КАК_ЗАПУСТИТЬ_ПРОЕКТ.md
│   ├── 06_ЛОГ_ИЗМЕНЕНИЙ.md
│   ├── BUGS.md
│   ├── QUALITY_RULES.md
│   ├── RELEASES.md
│   ├── LINKS.md
│   ├── ROLES_AND_OWNERS.md
│   ├── WORK_PROTOCOL.md
│   ├── RISKS_AND_DEPENDENCIES.md
│   └── FAQ_И_ГРАБЛИ.md
│
├── TASKS/                   # Задачи для исполнителей
│   ├── TASK_TEMPLATE.md
│   └── TASK-001.md ... TASK-0XX.md
│
├── SPECS/                   # Технические спецификации
│   ├── TECH_STACK.md
│   ├── DATABASE_SCHEMA.md
│   ├── API_TELEGRAM.md
│   └── ANALYTICS_SPEC.md
│
├── RESEARCH_BANK/           # Результаты исследований
│   └── design/              # Дизайн-исследования
│
├── design/                  # Дизайн-аудит (от агентов)
│   ├── README.md
│   ├── 00_CRITICAL_ISSUES.md
│   ├── 01_DESIGN_TOKENS.md
│   ├── 02_QUICK_WINS.md
│   ├── HANDOFF.md
│   └── CRITICAL_REVIEW.md
│
├── USER_FLOWS/              # Пользовательские сценарии
│   └── (будет заполнено)
│
├── ANALYTICS/               # Настройки аналитики
│   └── (будет заполнено)
│
├── app/                     # КОД ПРИЛОЖЕНИЯ
│   ├── components/          # UI компоненты
│   │   ├── StartScreen.jsx           (193 строки)
│   │   ├── QuestionScreen.jsx        (355 строк)
│   │   ├── ProgressBar.jsx           (235 строк)
│   │   ├── CompletionScreen.jsx      (143 строки)
│   │   └── ResultsScreen.jsx         (1361 строка) ← САМЫЙ БОЛЬШОЙ!
│   │
│   ├── data/                # Данные
│   │   ├── questions.js              # 25 вопросов + рекомендации
│   │   ├── gamification.js           # Чекпоинты + мотивашки
│   │   └── config.js                 # Telegram config (читает .env)
│   │
│   ├── utils/               # Утилиты
│   │   ├── calculations.js           # Расчёт results
│   │   └── telegram.js               # Отправка в TG
│   │
│   ├── layout.jsx           # Root layout + --app-height fix
│   └── page.jsx             # Главная логика (92 строки)
│
├── public/                  # Статические файлы
│   └── icons/
│       ├── flag.png         # 🏁 Чекпоинт 1
│       ├── bicep.png        # 💪 Чекпоинт 2
│       ├── shield.png       # 🛡️ Чекпоинт 3
│       └── trophy.png       # 🏆 Чекпоинт 4
│
├── .env.local               # Секреты (НЕ в Git!)
├── .gitignore
├── package.json
├── next.config.js
│
└── [Старая документация в корне]
    ├── PROJECT_BIBLE.md
    ├── ROADMAP.md
    ├── IMPLEMENTATION_PLAN.md
    └── (другие .md файлы)
```

---

## 🔢 СТАТИСТИКА КОДА

**Всего файлов:** 12
**Всего строк кода:** ~2500

**По компонентам:**
- ResultsScreen.jsx: 1361 строка (54% кода!)
- QuestionScreen.jsx: 355 строк
- ProgressBar.jsx: 235 строк
- StartScreen.jsx: 193 строки
- CompletionScreen.jsx: 143 строки
- questions.js: ~600 строк (25 вопросов)
- Остальное: ~600 строк

---

## 🎨 ЭКРАНЫ ПРИЛОЖЕНИЯ

### 1. StartScreen.jsx (Стартовый экран)

**Что показывает:**
- Заголовок: "Краш-тест маркетинга"
- Описание теста
- 4 иконки чекпоинтов (flag → bicep → shield → trophy)
- Кнопка "Начать аудит →"

**Особенности:**
- Декоративные blur-круги на фоне
- Градиентная кнопка (зеленая)
- Булиты с точками (11px)

---

### 2. QuestionScreen.jsx (Экран вопросов)

**Два типа вопросов:**

#### A) DiagnosticQuestion (22 вопроса)
- Вопрос с текстом
- 2 кнопки: "Да" (зеленая) / "Нет" (красная)
- Прогресс-бар сверху
- Мотивационные сообщения (синие/желтые плашки)

#### B) QualificationQuestion (3 вопроса)
- К1: текстовое поле (textarea)
- К2: множественный выбор (2-3 опции + "свой вариант")
- К3: единичный выбор (6 опций)
- Кнопка "Продолжить →" (активна после заполнения)

**Структура (3 блока по вертикали):**
1. Прогресс-бар (padding 16px)
2. Мотивашки (minHeight 70px)
3. Вопрос + кнопки (flex: 1, кнопки внизу)

**Критичные моменты:**
- `minHeight: 'var(--app-height, 100vh)'` — фикс для мобильных
- `overflow: 'auto'` — скролл контента
- `minHeight: 70px` для мотивашек — предотвращает прыжки

---

### 3. ProgressBar.jsx (Прогресс-бар)

**Элементы:**
- Цветная линия (зеленая → желтая → красная)
- 4 чекпоинта на позициях: 0%, 44%, 76%, 100%
- Иконки чекпоинтов (активируются после прохождения)
- Текущий прогресс (процент)

**Чекпоинты:**
```javascript
0%   (вопрос 1)  → 🏁 Предприниматель
44%  (вопрос 11) → 💪 Стойкий основатель
76%  (вопрос 19) → 🛡️ Железная воля
100% (вопрос 25) → 🏆 Целеустремлённый лидер
```

**Мотивационные сообщения:**
- Вопрос 2, 6, 15, 23 — синие плашки
- Вопрос 10, 18, 24 — желтые плашки (перед чекпоинтом)

---

### 4. CompletionScreen.jsx (Экран завершения)

**Что показывает:**
- Кубок 🏆 с анимацией pulse
- "Поздравляем!"
- "Вы прошли тест до конца"
- Бейдж ранга (золотой градиент)
- Кнопка "Смотреть результаты" (золотая)

---

### 5. ResultsScreen.jsx (Экран результатов) — ГЛАВНЫЙ!

**Структура:**

#### Header
- "ВАШИ РЕЗУЛЬТАТЫ"
- "Краш-тест маркетинга · Method ZMS"
- Подсказка с ❗ (кликать на блоки)

#### 3 секции результатов
1. **СТРАТЕГИЧЕСКИЙ МАРКЕТИНГ** (синий, 10 блоков)
2. **ГЕНЕРАЦИЯ ЛИДОВ** (оранжевый, 7 блоков)
3. **ПРОДАЖИ** (зеленый, 5 блоков)

**Каждая секция содержит:**
- Плашка статистики: "Работает на X%", "Уровень Y из 3"
- Блоки результатов (зеленые/красные карточки)
- Разбивка по уровням (0, 1, 2, 3)

#### PersonalRecommendations
- 5 приоритетных задач (из красных блоков)
- Нумерованный список

#### BonusSection
- 3 бонуса (Миро-схема, Голосовые, Видео-урок)
- Форма: Имя + Telegram + Email (опц.)
- Кнопка "Забрать бонусы" (зеленая)

#### PaidAuditSection
- Оффер: "Живой аудит с создателем метода ЗМС"
- Цена: 2999₽ вместо ~~7000₽~~
- Кнопка "Ухватить возможность" (оранжевая)

**Особенности:**
- Desktop: горизонтальные дорожки (scroll)
- Mobile (< 900px): вертикальные стеки
- Каскадная логика уровней (строки 423-442) — НЕ ТРОГАТЬ!

---

## 🔄 ЛОГИКА ПРИЛОЖЕНИЯ (page.jsx)

### Состояния (useState):
```javascript
screen: 'start' | 'testing' | 'completion' | 'results'
currentQuestionIndex: 0-24
answers: { questionId: answer }
results: { sections, blocks, recommendations }
justReachedCheckpoint: { question, title, icon } | null
```

### Переходы:
```
start → testing (handleStart)
testing → completion (после 25 вопроса)
completion → results (handleViewResults)
results → start (handleRestart)
```

### Функции:
- `handleStart()` — начать тест
- `handleAnswer(answer)` — сохранить ответ, следующий вопрос
- `handleViewResults()` — показать результаты
- `handleRestart()` — пройти заново

---

## 📊 ДАННЫЕ (data/)

### questions.js — 25 вопросов

**Структура вопроса:**
```javascript
{
  id: 'С1',                    // Уникальный ID
  section: 'Стратегия',        // Раздел
  level: 0,                    // Уровень (0-3)
  title: 'Интервью с клиентами',
  yes_rec: '...',              // Рекомендация если Да
  no_rec: '...',               // Рекомендация если Нет
  type: 'diagnostic',          // Тип
  questionNumber: 2            // Номер в тесте
}
```

**Порядок вопросов (allQuestions):**
```
#1  К1  (квалификационный, text)
#2-11  С1-С10  (диагностические, Стратегия)
#12 К2  (квалификационный, multiselect)
#13-19 Г1-Г7  (диагностические, Лидген)
#20 К3  (квалификационный, select)
#21-25 П1-П5  (диагностические, Продажи)
```

**Критично:** НЕ менять порядок без понимания логики сборки!

---

### gamification.js — Геймификация

**checkpoints:**
```javascript
[
  { question: 1, title: 'Предприниматель', icon: '🏁' },
  { question: 11, title: 'Стойкий основатель', icon: '💪' },
  { question: 19, title: 'Железная воля', icon: '🛡️' },
  { question: 25, title: 'Целеустремлённый лидер', icon: '🏆' }
]
```

**motivationalMessages:**
- Вопросы 2, 6, 15, 23

**preCheckpointMessages:**
- Вопросы 10, 18, 24

---

### config.js — Конфигурация

```javascript
export const TELEGRAM_CONFIG = {
  botToken: process.env.TELEGRAM_BOT_TOKEN,
  chatId: process.env.TELEGRAM_CHAT_ID,
};
```

**Секреты в .env.local:**
```
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHAT_ID=...
```

---

## 🛠️ УТИЛИТЫ (utils/)

### calculations.js — Расчёт результатов

**Функция:** `calculateResults(answers, questions)`

**Возвращает:**
```javascript
{
  sections: {
    'Стратегия': {
      blocks: [...],           // Массив блоков с status
      levels: [0, 1, 2, 3],
      totalGreen: 7,
      totalRed: 3,
      total: 10
    },
    'Лидген': {...},
    'Продажи': {...}
  },
  overallPercentage: 65,       // Общий процент
  firstRedBlock: {...}         // Первый красный блок (приоритет)
}
```

---

### telegram.js — Отправка в Telegram

**Функция:** `sendToTelegram(contactData, answers, results)`

**Отправляет:**
- Контактные данные
- Ответы на квалификационные вопросы
- Общий процент
- Статистика по секциям
- Проблемные зоны (до 10)

**Использует:** Telegram Bot API
**Endpoint:** `https://api.telegram.org/bot{token}/sendMessage`

---

## 🎨 ЦВЕТОВАЯ ПАЛИТРА

### Секции:
```javascript
'Стратегия': { bg: '#5B9BD5', light: '#7BB3E0', darker: '#4A8BC5' }
'Лидген':    { bg: '#ED8936', light: '#F6AD55', darker: '#DD7926' }
'Продажи':   { bg: '#48BB78', light: '#68D391', darker: '#38AB68' }
```

### Статусы блоков:
```javascript
// Зелёный (success):
bgColor: '#dcfce7', borderColor: '#86efac', textColor: '#166534'

// Красный (error):
bgColor: '#fee2e2', borderColor: '#fca5a5', textColor: '#991b1b'
```

### Основные цвета:
- Фон тёмный: `#0a0a0a`
- Фон светлый: `#f5f5f5`
- Зелёный: `#22c55e`
- Красный: `#ef4444`
- Жёлтый: `#eab308`
- Синий: `#4299e1`
- Золотой: `#ffd700`

---

## 🚨 КРИТИЧЕСКИЕ МЕСТА — НЕ ТРОГАТЬ!

### 1. --app-height в layout.jsx
```javascript
// Фикс для мобильных (iOS Safari 100vh bug)
<script dangerouslySetInnerHTML={{...}} />
```

### 2. Каскадная логика уровней (ResultsScreen.jsx:423-442)
```javascript
function getCurrentLevel(blocks) {
  // Сложная логика — НЕ ТРОГАТЬ!
}
```

### 3. Порядок вопросов (questions.js:246-253)
```javascript
export const allQuestions = [
  // Сложная сборка с slice — НЕ МЕНЯТЬ!
]
```

### 4. overflow: 'auto' в контейнерах вопросов
- Без этого контент не скроллится на мобиле

### 5. minHeight: 70px в блоке мотивашек
- Предотвращает прыжки контента

### 6. z-index тултипов (1000/1001)
- Без этого тултипы перекрываются

---

## 📦 ЗАВИСИМОСТИ

```json
{
  "dependencies": {
    "next": "14.0.4",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
```

**Минималистичный стек:** Только Next.js + React, без лишних библиотек.

---

## 🔗 СВЯЗИ МЕЖДУ ФАЙЛАМИ

```
page.jsx
  ├─ импортирует → StartScreen
  ├─ импортирует → QuestionScreen (Diagnostic + Qualification)
  ├─ импортирует → CompletionScreen
  ├─ импортирует → ResultsScreen
  ├─ использует → questions.js (allQuestions)
  ├─ использует → gamification.js (checkpoints)
  └─ использует → calculations.js (calculateResults)

ResultsScreen
  └─ использует → telegram.js (sendToTelegram)

QuestionScreen
  └─ использует → ProgressBar

telegram.js
  └─ использует → config.js (TELEGRAM_CONFIG)

config.js
  └─ читает → .env.local (process.env)
```

---

**Версия:** 1.0
**Дата создания:** 2026-01-25
**Источники:** PROJECT_BIBLE.md + актуальный код
