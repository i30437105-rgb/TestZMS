# 🚀 КАК ЗАПУСТИТЬ ПРОЕКТ

> Инструкция для разработчика

---

## ⚡ БЫСТРЫЙ СТАРТ

```bash
# 1. Клонировать репозиторий (если нужно)
git clone [URL]
cd TestZMS

# 2. Установить зависимости
npm install

# 3. Создать .env.local (если нет)
# Скопировать из .env.example или создать вручную

# 4. Запустить dev сервер
npm run dev

# → Открыть http://localhost:3000
```

---

## 📋 ТРЕБОВАНИЯ

**Node.js:** 18.x или выше
**npm:** 9.x или выше
**OS:** Windows / macOS / Linux

**Проверка версий:**
```bash
node --version   # должно быть >= 18
npm --version    # должно быть >= 9
```

---

## 🔐 НАСТРОЙКА .env.local

**КРИТИЧНО:** Без этого файла Telegram интеграция не работает!

### 1. Создать файл .env.local в корне проекта:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

### 2. Получить токены:

**Telegram Bot Token:**
1. Открыть Telegram, найти @BotFather
2. Отправить `/newbot` или использовать существующего бота
3. Скопировать токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Telegram Chat ID:**
1. Открыть Telegram, найти бота @userinfobot
2. Отправить `/start`
3. Скопировать "Id" (формат: `123456789`)

### 3. Проверка:
```bash
# После создания .env.local перезапустить dev сервер
npm run dev
```

---

## 📦 КОМАНДЫ

### Разработка:
```bash
npm run dev
# Запуск dev сервера на http://localhost:3000
# Hot reload включен (изменения применяются автоматически)
```

### Сборка:
```bash
npm run build
# Создает production сборку в .next/
# Проверяет ошибки TypeScript/ESLint (если настроено)
```

### Production:
```bash
npm run start
# Запускает production сервер (требует npm run build перед этим)
```

---

## 🔍 ПРОВЕРКА РАБОТОСПОСОБНОСТИ

### 1. Dev сервер запустился?
```bash
npm run dev

# Ожидаемый вывод:
# ✓ Ready in 2.5s
# ○ Local:    http://localhost:3000
```

### 2. Открыть http://localhost:3000

**Ожидаемое:**
- ✅ Отображается StartScreen
- ✅ Кнопка "Начать аудит" работает
- ✅ Нет ошибок в консоли браузера (F12)

**Если ошибки:**
- Проверь .env.local (токены заполнены?)
- Проверь консоль (npm run dev) — есть ли ошибки?
- Проверь package.json — все зависимости установлены?

### 3. Пройти тест до конца

**Чек-лист:**
- [ ] Вопросы отображаются корректно
- [ ] Прогресс-бар обновляется
- [ ] Результаты рассчитываются
- [ ] Форма бонусов показывается
- [ ] Отправка в Telegram работает (проверить Telegram бот)

---

## 🛠️ TROUBLESHOOTING

### Ошибка: "Cannot find module 'next'"
**Решение:**
```bash
rm -rf node_modules package-lock.json
npm install
```

---

### Ошибка: "process.env.TELEGRAM_BOT_TOKEN is undefined"
**Решение:**
1. Проверить что `.env.local` существует
2. Проверить что переменные записаны БЕЗ кавычек:
   ```env
   # ✅ ПРАВИЛЬНО:
   TELEGRAM_BOT_TOKEN=123456:ABC

   # ❌ НЕПРАВИЛЬНО:
   TELEGRAM_BOT_TOKEN="123456:ABC"
   ```
3. Перезапустить `npm run dev`

---

### Ошибка: "Port 3000 already in use"
**Решение:**
```bash
# Убить процесс на порту 3000
# Windows:
netstat -ano | findstr :3000
taskkill /PID [PID] /F

# macOS/Linux:
lsof -ti:3000 | xargs kill
```

**Или использовать другой порт:**
```bash
PORT=3001 npm run dev
```

---

### Кнопки работают, но Telegram не получает сообщения
**Проверить:**
1. Токены в `.env.local` корректные?
2. Открыть DevTools (F12) → Network → Посмотреть запрос к Telegram API
3. Если статус 401 → токен неверный
4. Если статус 403 → бот заблокирован или chat_id неверный

---

### Страница белая (ничего не отображается)
**Проверить:**
1. Консоль браузера (F12) — есть ошибки JavaScript?
2. Консоль терминала (npm run dev) — есть ошибки сборки?
3. Проверить `app/page.jsx` — нет синтаксических ошибок?

---

## 📱 ТЕСТИРОВАНИЕ НА МОБИЛЬНОМ

### Вариант 1: DevTools (быстро, но не точно)
1. Открыть http://localhost:3000
2. F12 → Toggle device toolbar (Ctrl+Shift+M)
3. Выбрать iPhone/Android preset
4. Протестировать

---

### Вариант 2: Реальное устройство (точно)

**Если компьютер и телефон в одной WiFi сети:**

1. Узнать IP компьютера:
   ```bash
   # Windows:
   ipconfig

   # macOS/Linux:
   ifconfig
   ```

2. Открыть на телефоне:
   ```
   http://[IP]:3000
   # Например: http://192.168.1.100:3000
   ```

3. Протестировать прохождение теста

---

## 🔥 ГОРЯЧИЕ КЛАВИШИ

**Dev режим:**
- `Ctrl+C` — остановить сервер
- `Ctrl+Shift+R` — hard refresh (очистить кэш браузера)
- `F12` — открыть DevTools

**В коде:**
- После изменения `.env.local` — ПЕРЕЗАПУСТИТЬ сервер!
- После изменения `.jsx` файлов — автоматический reload

---

## 🚀 ДЕПЛОЙ НА VERCEL

### 1. Подготовка:
```bash
# Проверить что проект собирается
npm run build

# Проверить что .gitignore настроен
cat .gitignore
# Должны быть: .env.local, node_modules, .next
```

### 2. Deплой:
```bash
# Вариант A: Через Vercel CLI
npm i -g vercel
vercel

# Вариант B: Через Git + Vercel Dashboard
# 1. Push в GitHub
# 2. Подключить репозиторий в Vercel Dashboard
```

### 3. Добавить env переменные в Vercel:
1. Зайти в Project Settings → Environment Variables
2. Добавить:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
3. Redeploy

---

## 📝 CHECKLIST ПЕРЕД ДЕПЛОЕМ

- [ ] `npm run build` успешно
- [ ] `.env.local` в `.gitignore`
- [ ] Токены НЕ в коде (только в .env)
- [ ] Протестировано на Desktop
- [ ] Протестировано на Mobile
- [ ] Отправка в Telegram работает
- [ ] Нет критических багов
- [ ] Все задачи Фазы 1 завершены

---

**Версия:** 1.0
**Дата создания:** 2026-01-25
